import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    button: {
        width: "30%",
        height: 52,
        backgroundColor: "#eaeaea",
        borderRadius: 8,

        borderWidth: 1,
        borderColor: "#000000",

        justifyContent: "center",
        alignItems: "center",

        boxShadow: "0px 2px 8px rgba(150, 150, 150, 0.67)",
    },

    title: {
        color: "#000000",
        fontSize: 16,
        fontWeight: "bold",
    },
});